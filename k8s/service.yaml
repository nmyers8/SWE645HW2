apiVersion: v1
kind: Service
metadata:
  name: my-static-web-app
  labels:
    run: my-static-web-app
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80   
    protocol: TCP
    name: http
  - port: 443
    protocol: TCP
    name: https
  selector:
    run: my-static-web-app
